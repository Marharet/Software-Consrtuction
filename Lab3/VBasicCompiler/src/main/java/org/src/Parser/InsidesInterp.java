package org.src.Parser;

/**
 * Enumeration of inside interpretation of keywords
 */
public enum InsidesInterp {
    UNKNCOM,
    PRINT,
    INPUT,
    IF,
    AND,
    OR,
    THEN,
    FOR,
    NEXT,
    TO,
    GOTO,
    GOSUB,
    RETURN,
    END,
    EOL,
}
